#!/usr/bin/env bash

# Auto-generated test/run-all wrapper script
# Generator: build-generate
# DO NOT EDIT MANUALLY

set -euo pipefail

whereami=$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")

(
    cd "${whereami}/.." && docker run --rm --interactive \
        -v "${PWD}:${PWD}" \
        -v /tmp:/tmp \
        -e "IMAGE=${IMAGE:-}" \
        --workdir "${PWD}" \
        --entrypoint sh \
        "bats/bats:1.13.0" \
        -c "apk add --quiet --no-cache >/dev/null 2>&1 && bats test"
)
