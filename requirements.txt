# Transitive dependency patches applied over rotate-backups' resolved deps.
# Minimum constraints: raise the floor here when CVE fixes are released.
# Dependabot tracks this file and opens version-bump PRs automatically.
# Applied to both the pip-managed venv and the system Python interpreter.
cryptography >= 47.0.0
idna >= 3.7
setuptools >= 78.1.0
urllib3 >= 2.6.3
zipp >= 3.19.1
